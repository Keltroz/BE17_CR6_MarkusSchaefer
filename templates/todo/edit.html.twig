{% extends 'base.html.twig' %}

{% block title %}Edit Library{% endblock %}

{% block body %}
    <h1>Edit Library</h1>

    {{ include('todo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_todo_index') }}" class="btn btn-primary" style="width: 70px">Back</a>

    {{ include('todo/_delete_form.html.twig') }}
{% endblock %}
